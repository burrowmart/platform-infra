{
  "name": "@demo/saga-integration-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-service integration suite for the order-placement saga (order-service + catalog-service + payment-service) over real Docker containers — no mocks.",
  "scripts": {
    "compose:up": "docker compose -f ../docker-compose.yml -f ../docker-compose.test.yml up -d --build",
    "compose:down": "docker compose -f ../docker-compose.yml -f ../docker-compose.test.yml down",
    "compose:logs": "docker compose -f ../docker-compose.yml -f ../docker-compose.test.yml logs -f order-service catalog-service payment-service",
    "test": "jest --runInBand --forceExit",
    "test:once": "npm run compose:up && npm run test"
  },
  "dependencies": {
    "amqplib": "^2.0.1",
    "mongodb": "^6.20.0"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.7",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.12.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.4",
    "typescript": "^5.4.0"
  }
}
