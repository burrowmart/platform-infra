{
  "name": "@demo/acceptance",
  "version": "0.1.0",
  "private": true,
  "description": "Release-engineering acceptance harness for ARCHITECTURE.md's six acceptance criteria. Runs the full compose stack (saga participants + notification-service + observability profile), asserts each criterion from this harness (reusing integration-tests' support libs and opa-policies/demo where the criterion says to), and emits ACCEPTANCE.md with evidence.",
  "scripts": {
    "run": "tsx run-acceptance.ts"
  },
  "dependencies": {
    "amqplib": "^2.0.1",
    "ioredis": "^5.4.1",
    "mongodb": "^6.20.0"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.7",
    "@types/node": "^20.12.0",
    "tsx": "^4.19.0",
    "typescript": "^5.4.0"
  }
}
