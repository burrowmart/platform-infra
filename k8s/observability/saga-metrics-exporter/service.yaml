apiVersion: v1
kind: Service
metadata:
  name: saga-metrics-exporter
  namespace: default
spec:
  selector:
    app: saga-metrics-exporter
  ports:
    - name: metrics
      port: 9308
      targetPort: 9308
